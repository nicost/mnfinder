import java.awt.Rectangle;

import ij.plugin.frame.RoiManager;
import ij.gui.ROI;

rm = RoiManager.getInstance2();
for (roi : rm.getRoisAsArray()) {
	rc = roi.getBounds();
	xc = rc.x + 0.5 * rc.width;
	yc = rc.y + 0.5 * rc.height;
	gui.message("roi: " + xc + ", " + yc);
}